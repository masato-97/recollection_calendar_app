<div class="container">
  <div class="row">
    <div class="col-lg-8 offset-lg-2">
      <h1>post編集</h1>

      <%= form_with model: @post, url: memory_post_path(@memory, @post), local: true do |f| %>
        <div class="mb-3">
          <%= f.label :title %>
          <%= f.text_field :title, class: "form-control" %>
        </div>

        <div class="mb-3">
          <%= f.label :body %>
          <%= f.text_area :body, class: "form-control", rows: "10" %>
        </div>

        <%= f.submit nil, class: "btn btn-primary mb-3" %>
      <% end %>

      <div class="mb-3">
        <div>※表示される画像（思い出のメイン画像）</div>
        <%= image_tag @memory.main_image_url, width: "300", height: "200" %> <!-- imageカラムを表示 -->
      </div>

      <% if @memory.score.present? %>
        <div>※表示される評価</div>
        <div class="content average-score">
          <div class="star-rating mb-2">
            <div class="star-rating-front" style="width: <%= @memory.score * 20 %>%">★★★★★</div>
            <div class="star-rating-back">★★★★★</div>
          </div>
          <div class="average-score-display ml-3 pt-2">
            <%= @memory.score %>/5
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>